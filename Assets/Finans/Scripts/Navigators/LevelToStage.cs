using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class LevelToStage : MonoBehaviour
{
    [SerializeField] public GameObject transitionBox;
    [SerializeField] public GameObject plainBox;

    [SerializeField] Button nextTansitionButton;
    [SerializeField] Button nextPlainButton;
    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}

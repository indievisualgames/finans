using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FTEndScreenScore : MonoBehaviour
{

    int correctAnswer;
    int incorrectAnswer;
    bool havePendings;
    int pendingCorrectAnswer;
    int pendingIncorrectAnswer;
    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
